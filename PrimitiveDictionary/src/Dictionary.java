public class Dictionary {
    protected Word[] arr;

    /**
     * Initialize a word array with size.
     * @param size is the length of the array.
     */
    Dictionary(int size) {
        arr = new Word[size];
    }
}

